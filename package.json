{
  "name": "napsak-app",
  "description": "N'apsak? Ankara aktivite ve mekan öneri uygulaması.",
  "private": true,
  "version": "0.1.0",
  "main": "index.ts",
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "expo": "~57.0.7",
    "expo-location": "~57.0.7",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-native": "0.86.0",
    "react-native-safe-area-context": "~5.7.0"
  },
  "devDependencies": {
    "@types/react": "~19.2.2",
    "typescript": "~6.0.3"
  },
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf .test-build && tsc src/domain.ts src/types.ts src/recommendations.ts src/data/places.ts src/persistence.ts --ignoreConfig --esModuleInterop --moduleResolution node16 --outDir .test-build --module Node16 --target es2020 --skipLibCheck && node --test tests/domain.test.cjs && rm -rf .test-build",
    "test:stress": "rm -rf .test-build && tsc src/domain.ts src/types.ts src/recommendations.ts src/data/places.ts src/persistence.ts --ignoreConfig --esModuleInterop --moduleResolution node16 --outDir .test-build --module Node16 --target es2020 --skipLibCheck && node tests/recommendation-stress.cjs && rm -rf .test-build"
  }
}
